# `tests/runtime`

Focused coverage for runtime and session materialization behavior.

## What This Part Covers

- meta-session seeding and alignment
- `gene` initialization
- session creation from entities
- watcher startup and runtime integration

## How It Contributes To The Whole System

These tests protect the part of the system that turns config on disk into running, persistent sessions.

