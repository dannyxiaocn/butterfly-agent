# openai_agent

OpenAI-backed provider variant of the default agent.

## Purpose
- use the standard `agent` capability surface with an OpenAI provider
- useful for coding / reasoning tasks where OpenAI is preferred

## Notes
- intentionally thin wrapper over `agent`
- retained as an active built-in provider-specific entity
