{
  "name": "archive_session",
  "description": "Archive a session by moving its sessions/ and _sessions/ directories into _archived/ and recording archive metadata.",
  "input_schema": {
    "type": "object",
    "properties": {
      "session_id": {
        "type": "string",
        "description": "Target session ID to archive."
      },
      "reason": {
        "type": "string",
        "description": "Optional archival reason."
      }
    },
    "required": ["session_id"]
  }
}
