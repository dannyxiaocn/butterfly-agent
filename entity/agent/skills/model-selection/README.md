# entity/agent/skills/model-selection

This directory is currently an inactive placeholder.

## What It Is

No skill is shipped here today.

## How To Use It

Add a `SKILL.md` only if model-selection guidance needs to become part of the base entity's default skill set.

## How It Fits

It is a reserved path, not an active built-in skill.
