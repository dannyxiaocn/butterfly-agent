# entity/agent/skills/messaging

This directory is currently an inactive placeholder.

## What It Is

No skill is shipped here today.

## How To Use It

Only add a real `SKILL.md` here if the base entity should gain a messaging-specific skill by default.

## How It Fits

Keeping the directory documented makes it clear that this path is reserved, not a missing runtime capability.
