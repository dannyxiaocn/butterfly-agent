# entity/agent/skills/multi-agent

This directory is currently an inactive placeholder.

## What It Is

No skill is shipped here today.

## How To Use It

Populate it only when multi-agent workflow guidance should be exposed from the base entity by default.

## How It Fits

The directory exists as a reserved extension point rather than a live capability.
