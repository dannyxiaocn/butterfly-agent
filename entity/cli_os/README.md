# cli_os

CLI-focused exploration specialist.

## Purpose
- immersive shell and filesystem workflows
- exploratory coding, debugging, and environment inspection
- fewer coordination tools than the default `agent`

## Notes
- extends `agent` but narrows the tool surface toward direct shell work
- intended for tasks where an unrestricted CLI mindset is useful
