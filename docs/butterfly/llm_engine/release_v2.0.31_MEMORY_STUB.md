# Proposed MEMORY.md update — v2.0.31

## 1-line index entry (for the "Meta" / "Current main" block)

Copy this verbatim under `**Current main: v2.0.31 (...)**` (or under the existing `v2.0.29` bullet, following the same pattern):

```
- **v2.0.31 (PR TBD, branch `v2.0.31-llm-engine`, 1018/1018)**: LLM-engine five-phase overhaul. Phase 1A — `models.yaml` + `ModelSpec` gained 7 optional per-model fields (`thinking_{mode,effort,display,budget_tokens}`, `interleaved_thinking_beta`, `cache_{strategy,ttl}`); absent keys → `None` on spec → providers fall through to legacy shapes. Phase 1B — 37 occurrences of `kimi-k2-turbo-preview`/`kimi-k2.5`/`kimi-k2-0711-preview` renamed to the rolling alias `kimi-for-coding`. Phase 2 — `anthropic.py` consults spec: adaptive thinking (`thinking={type:"adaptive",display:...}` + `output_config={effort:...}`) via new `_apply_thinking_kwargs`; three cache strategies (`single`/`two_plus_two`/`auto`) via `_apply_cache_strategy` + 3 helpers; `cache_ttl` stamped on each cache_control dict; legacy shape regression pinned via `_DEFAULT_CACHE_CONTROL_SENTINEL`; `KimiAnthropicProvider._supports_adaptive_thinking=False` so Kimi always takes legacy branch. Phase 3 — `codex._get_auth_async` now prefers persisted `account_id` (re-extracted from fresh JWT on refresh, legacy JWT fallback still works); `codex.py`+`openai_responses.py` gained `_CHUNK_IDLE_TIMEOUT=45.0` SSE watchdog (codex per-chunk bytes, responses per-SDK-event) raising `ProviderTimeoutError` instead of waiting out 600-s read timeout. Phase 4 — `butterfly kimi login` rewritten: Moonshot KLIP-14 device flow (`auth.kimi.com/api/oauth/{device_authorization,token}`, client_id `17e5f671-d194-4dfb-9706-5516cb48c098`), stores `access_token` as `KIMI_FOR_CODING_API_KEY` + `refresh_token` as `KIMI_REFRESH_TOKEN` in `.env`; auto fast-path when key already set; `--oauth` forces OAuth, `--api-key` forces legacy getpass; `slow_down` doubles interval (cap 15 s); `access_denied`/`expired_token` fail-fast; device-id = `sha256(hostname+platform.platform())[:16]`; every exit path (success/fail/skip/Ctrl-C) prints 3-line agent-hint via `try/finally`. Phase 5 — Codex+OpenAI-Responses SSE streams return 6-tuple `(text, tool_calls, usage, reasoning_items, builtin_items, builtin_progress)`; handles `web_search / file_search / code_interpreter / image_generation / mcp / computer_call` SSE families; `_classify_builtin_event` + `_BUILTIN_EVENT_RE` shared; `code_interpreter_call.code.delta` accumulated per `item_id` and spliced onto captured item; `_convert_assistant` replays captured built-in items verbatim (stripping `None`-valued and `_`-prefixed keys). New provider methods `consume_builtin_tool_events()` (progress drain for UI) + `consume_extra_blocks()` extended to fold built-in items on top of reasoning. `core/tool.py::Tool` gained `builtin_dict` ctor kwarg + `to_builtin_dict()` + `is_builtin`; `tool_engine/loader.py::_load_builtin_dict` reads module-level class attr; three toolhub stubs ship (`toolhub/{web_search,file_search,code_interpreter}/`), each `execute()` raises `NotImplementedError("...provider-native built-in tool...")`; `image_generation` + `computer_use` parsed but no stubs. Anthropic YAML defaults: `thinking_mode:adaptive`, `thinking_effort:high`, `thinking_display:summarized`, `thinking_budget_tokens:8000`, `interleaved_thinking_beta:false`, `cache_strategy:two_plus_two`, `cache_ttl:5m`. See [release_v2.0.31.md](release_v2.0.31.md).
```

The line is ~3100 chars. If the harness's 200-char per-index-line budget matters, collapse to the shorter form below and rely on the linked release file for detail:

```
- **v2.0.31 (branch `v2.0.31-llm-engine`, 1018/1018)**: LLM-engine 5-phase — 7 opt-in `models.yaml` fields (thinking_mode/effort/display/budget, interleaved_beta, cache_strategy/ttl) → Anthropic adaptive + single/two_plus_two/auto cache ladder; Kimi model name → `kimi-for-coding`; codex auth prefers persisted `account_id` + 45-s SSE watchdog; `butterfly kimi login` → Moonshot KLIP-14 device flow; Codex+Responses 6-tuple stream adds built-in `web_search/file_search/code_interpreter/...` items+progress; `Tool.builtin_dict` + 3 toolhub stubs. See [release_v2.0.31.md](release_v2.0.31.md).
```

## Proposed detail paragraph (for a topic file)

If you split v2.0.31 out of MEMORY.md into its own topic file (mirroring `release_v2.0.25.md` et al.), drop this into `projects/-Users-xiaobocheng-agent-core-butterfly-agent/memory/release_v2.0.31.md`:

```
v2.0.31 reshapes the LLM engine. models.yaml + ModelSpec pick up 7 optional per-model fields — all default to None on ModelSpec so unchanged YAML keeps legacy behavior. Anthropic's complete() now consults get_model_spec(model) and dispatches thinking via _apply_thinking_kwargs (adaptive = thinking:{type:adaptive,display:...}+output_config:{effort:...}; legacy = thinking:{type:enabled,budget_tokens:N} or extra_body for Kimi) + cache via _apply_cache_strategy (single/two_plus_two/auto with shared _resolve_cache_control stamping ttl). KimiAnthropicProvider pins _supports_adaptive_thinking=False so Kimi-served models always take legacy even with mode=adaptive in YAML. A _DEFAULT_CACHE_CONTROL_SENTINEL lets legacy call sites emit the exact pre-2.0.31 cache_control dict (no ttl key) so regression tests pin. Codex's _get_auth_async now prefers the persisted account_id field (_refresh_access_token_async re-extracts it from fresh JWT); legacy auth.json still works via JWT fallback. Both codex and openai_responses wrap their stream iterators in asyncio.wait_for(..., timeout=_CHUNK_IDLE_TIMEOUT=45.0) raising ProviderTimeoutError on stall. butterfly kimi login rewritten: auto-selects fast-path (key already set → verify), OAuth (Moonshot KLIP-14 device flow; client_id 17e5f671-d194-4dfb-9706-5516cb48c098; slow_down doubles interval cap 15s; access_denied/expired_token fail-fast; Ctrl-C exit 130), or legacy getpass (--api-key). OAuth persists access_token as KIMI_FOR_CODING_API_KEY + refresh_token as KIMI_REFRESH_TOKEN. Device-id = sha256(hostname+platform.platform())[:16]. Every exit path prints 3-line agent-hint via try/finally so LLM agents reading stdout don't spin OAuth forever. Codex + OpenAI-Responses _parse_sse_stream/_stream return 6-tuple (text, tool_calls, usage, reasoning_items, builtin_items, builtin_progress); six built-in item types captured (web_search_call/file_search_call/code_interpreter_call/image_generation_call/mcp_call/mcp_list_tools/computer_call); _convert_assistant replays captured items verbatim in next turn's input[] stripping None-valued + _-prefixed keys; code_interpreter code.delta accumulated per item_id and spliced onto captured item when server omits final code field. New provider methods consume_builtin_tool_events() (progress drain for UI) + consume_extra_blocks() extended to fold built-in items. Tool gained builtin_dict ctor kwarg + to_builtin_dict() + is_builtin property; three toolhub stubs ship (web_search/file_search/code_interpreter), each execute() raises NotImplementedError with a configuration-hint message; loader._load_builtin_dict reads a module-level builtin_dict class attr best-effort. image_generation + computer_use are parsed by the provider but no toolhub stub ships. Frontend types.ts + models_service.py re-export all 7 new fields for web UI editors. Full test suite 1018/1018 at HEAD. Anthropic YAML defaults: thinking_mode:adaptive, thinking_effort:high, thinking_display:summarized, thinking_budget_tokens:8000, interleaved_thinking_beta:false, cache_strategy:two_plus_two, cache_ttl:5m.
```

## Notes for the human consolidator

- The 1-line index entry reuses the phrasing and structure of the existing v2.0.29 bullet (Phase lettering, code-fence identifiers for helper names, colon-separated action/effect pairs). If you've recently tightened index lines, prefer the shorter form.
- The detail paragraph avoids newlines so it survives copy-paste into a bullet; split into sub-bullets if your current MEMORY.md scheme allows.
- Both forms omit PR number / merge SHA — fill those in when the PR lands. Branch is `v2.0.31-llm-engine`; pyproject version is `2.0.31`.
- The file you're reading is safe to delete after you've copied the bits you want into MEMORY.md / topic file. It's a scratch artifact for the consolidation step.
