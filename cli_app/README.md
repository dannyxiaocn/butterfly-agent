# cli_app

This directory is currently just a placeholder.

## What It Is

It is tracked only to reserve the path; the active CLI implementation lives in `ui/cli/`.

## How To Use It

Do not add new CLI behavior here unless the project intentionally reintroduces a separate app package.

## How It Fits

Keeping this directory explicit prevents confusion: it is not part of the current runtime path.
